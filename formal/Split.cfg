\* Split register-then-park (historical, pre-recheck): TLC MUST report a
\* NoStuckWaiter violation -- the lost wakeup. This config is a documented
\* NEGATIVE check: it is expected to fail, demonstrating the model catches the
\* exact bug class. The CI wrapper asserts the violation is found.
CONSTANT Atomic = FALSE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT NoStuckWaiter
